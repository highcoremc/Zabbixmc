version: '3.8'

services:
  frontend:
    image: registry.gitlab.com/zabbixmc/zabbixmc/app:latest
    command: ['nginx', '-g', 'daemon off;']
    volumes:
      - .env:/app/.env
    ports:
      - "8090:80"
